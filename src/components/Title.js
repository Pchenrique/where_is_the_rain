import React from "react";

class Title extends React.Component{
    render(){
        return(
            <div>
                <h1>Where is the rain?</h1>
            </div>
        );
    }
};

export default Title;